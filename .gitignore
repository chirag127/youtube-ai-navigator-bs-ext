# Node.js
/node_modules
/dist
/build
/coverage

# WXT-specific (if applicable)
/.wxt

# Environment variables
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# OS-specific
.DS_Store
Thumbs.db

# IDE specific files
.vscode/
.idea/
*.iml
*.suo
*.user
*.userosf
*.sln.docstates

# Test files
*.snap

# Compiled files / other generated content
*.js.map
*.d.ts.map

# Package Manager Specific
# Depending on which package manager is used, you might want to ignore other lock files.
# Typically, only one lock file (e.g., package-lock.json for npm) should be committed.
# If you switch package managers or have multiple present, ignore the ones not in use.
pnpm-lock.yaml
yarn.lock

# Browser Extension specific output
*.zip
*.crx
*.xpi
/extension # Common output folder name for built extensions
web-ext-data/ # Temporary profiles created by web-ext (Firefox extension testing tool)

# CI/CD specific generated files
/.github/workflows/*.json # For example, if workflow runs generate temporary JSON outputs

# Stray output files
eslint-output.json
eslint-tests.json
test-results.json
test_output*.txt
megalinter-reports/
mega-linter.log
